import React from "react";

const Variant = ({ elem }) => {
  return(
    <div className='variant-wrap'>
      <div className='variant'>
        {elem}
      </div>
    </div>
  );
};

export default Variant;